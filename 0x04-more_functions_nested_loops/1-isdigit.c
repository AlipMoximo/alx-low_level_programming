#include "main.h"
/**
 * _isdigit-checks for a digit 0 to 9
 * @c:is the digit
 * Return: 1 if c is a digit or 0 is not
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 56)
{
return (1);
}
return (0);
}
