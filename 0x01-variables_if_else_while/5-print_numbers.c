#include<stdio.h>
/**
 * main-Print all single digit numbers of base 10 starting with 0
 * Return:Always 0(Success)
 */
int main(void)
{
int m;
for (m = 0; m < 10; m++)
{
printf("%d", m);
}
printf("\n");
return (0);
}
